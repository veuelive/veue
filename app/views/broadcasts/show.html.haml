#broadcast{data: {controller: "broadcast", broadcast: {"stream-key": current_user.stream_key, state: current_broadcast_video.state, "video-id": current_broadcast_video.to_param}}}
  .content--left
    .video-bar
      .video-bar__address
        %input{type: "text", data: {controller: "broadcasts.address-bar", action: "keypress->broadcasts.address-bar#onKeypress", target: "broadcasts.address-bar.addressBar"}}
      .video-bar__button
        %button{data: {action: "click->broadcast#startStreaming", showOnState: "ready"}} Start Broadcast
        %button{data: {action: "click->broadcast#stopStreaming", showOnState: "live"}} Stop Broadcast
        %button.time-area{data: {showOnState: "live"}}
          %span.dot.time-dot
          \-:-:-
  .content--right
    %video#webcam_preview{data: {target: "broadcast.webcamVideoElement"}}
    .debug-area
      %canvas#debug_canvas{width: 1280, height: 1280, data: {target: "broadcast.debugCanvas"}}
